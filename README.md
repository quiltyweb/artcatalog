# Brushella

## Setup

- Install NVM
- `nvm use`
- `npm install`
- `cp .env.example .env`
  - Update `SHOPIFY_ADMIN_PASSWORD` and `SHOPIFY_STORE_URL` to match the values of a real Shopify account
- `npm run develop`
